# Помощник Опера v0.4

Два независимых инструмента:
- Проработка — таблица → Excel.
- Заявка + Доверенность — текст → разбор и проверка → Word-шаблон.

Логика инструментов разделена в `app.js`. Имя Проработки получает КТК без `/`.
