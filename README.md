# Помощник Опера v0.4.19

Патч:
- возвращено заполнение второй даты на правой странице;
- дата слева через явный ориентир «Дата:» сохранена;
- в заявке сохраняется подпись «г.н.з.» перед госномером;
- убраны дублирующие `№` в полях, где знак уже есть в шаблоне;
- убран дубль `КГ` в весе: в код передаётся только число, единица остаётся в шаблоне;
- добавлен пробел в местах, где значения вставляются сразу после «выдан» и после запятой;
- получатель не изменялся.
